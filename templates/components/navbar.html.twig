<nav class="container">
    <div class="logo">
        <img src="{{ asset('build/img/logo.png') }}" alt="Logo">
    </div>
    {% if app.user != null %}
        <div class="navigation">
            Logged as <span style="font-weight: 500">{{ app.user.email }}</span> |
            <a href="{{ path('add_action_app') }}"><i class="fa fa-add"></i> Add new task</a> |
            <a href="{{ path('app_logout') }}"><i class="fa-solid fa-arrow-right-from-bracket"></i> Logout</a>
        </div>
    {% else %}
        <div class="navigation">
            <a href="{{ path('app_login') }}"><i class="fa-solid fa-user"></i> Log in</a> |
            <a href="{{ path('app_register') }}"><i class="fa-solid fa-arrow-right-to-bracket"></i> Register</a>
        </div>
    {% endif %}
</nav>